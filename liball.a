!<arch>
0-isupper.o/    0           0     0     644     188       `
#include "main.h"

/**
 * _isupper - uppercase letters
 * @c: char to check
 *
 * Return: 0 or 1
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}

0-memset.o/     0           0     0     644     385       `
#include "main.h"

/**
 * _memset - Program fills a block memory with a specific value
 * @s: starting adress of memory to be filled 
 * @b: constant byte
 * @n: number of bytes of memory area to fill
 * 
 * Return: change array with new value for n bytes
 */

char *_memset(char *s, char b, unsigned int n)
{
	int a = 0;

	for  (; n > 0; a++)
	{
		a[i] = b;
		n--;
	}

	return (s);
}

